import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: "var(--font-quicksand-sans), 'Quicksand', Arial, sans-serif",
  },
});

export default theme;
